# Fetch every img element in the database
# Use positional data to re-extract the image from the source PDF stored in AWS, but with higher resolution
# Save the new image to AWS, overwriting the old one
